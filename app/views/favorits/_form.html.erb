<%= simple_form_for @favorit do |f| %>
  <%= f.input :name, label: 'Name: ' %>
  <%= f.input :url, label: 'Url: ' %>
  <%= f.input :description, label: 'Description: ' %>
  <%= f.input :link, label: 'Это сслыка? ' %>
  <%= f.submit 'Save' %>
<% end %>
